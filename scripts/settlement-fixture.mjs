import { decimal } from "../dist/index.js";
export const now = "2026-08-31T00:00:00Z";
export const capabilities = ["BROKER_FEE_SPLIT", "CONDITIONAL_RELEASE", "REFUND_ALLOCATION", "DISPUTE_FREEZE", "REVERSAL_EVENTS", "BANK_ACKNOWLEDGEMENT"];
export function approval(provider, environment="SANDBOX") { return { approvalId: `approval-${provider}`, provider, environment, writtenApprovalReceiptId: `receipt-${provider}`, actualUseCase: "synthetic polymer brokerage adapter fixture only", commodityFamilies: ["RPP_NATURAL_LIGHT_INJECTION"], currencies: ["INR", "USD"], minimumGross: decimal("1"), maximumGross: decimal("100000000"), capabilities, validFrom: "2026-08-01T00:00:00Z", validUntil: "2026-12-01T00:00:00Z", state: "APPROVED" }; }
export function credentials(provider, environment="SANDBOX") { return { provider, environment, state: "VALID", secretReference: `secret://fixture/${provider}`, verifiedAt: "2026-08-30T00:00:00Z" }; }
export function draft(provider, currency="INR") { return { instructionId: `instruction-${provider}`, tradeId: "trade-1", provider, environment: "SANDBOX", commodityFamily: "RPP_NATURAL_LIGHT_INJECTION", buyerId: "buyer-1", supplierId: "supplier-1", sablestoneBeneficiaryId: "sablestone-1", currency, grossAmount: decimal("8500000"), supplierEntitlement: decimal("8000000"), sablestoneEntitlement: decimal("500000"), otherAllocations: [], releaseConditions: ["buyer acceptance or contract condition"], disputeProcedure: "freeze under fixture provider procedure", expiresAt: "2026-09-01T00:00:00Z", idempotencyKey: `create:${provider}:trade-1` }; }
